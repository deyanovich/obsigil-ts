{
  "name": "obsigil-ts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "TypeScript implementation of the obsigil mandate-token format",
  "license": "MIT OR Apache-2.0",
  "packageManager": "pnpm@11.3.0",
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean && rm -rf packages/*/dist packages/*/tsconfig.tsbuildinfo",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -b"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
